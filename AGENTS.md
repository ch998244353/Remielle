# 蕾米埃尔桌宠 Agent 入口

开始任何工作前，完整阅读 [`项目交接/蕾米埃尔桌宠-统一项目解析与Agent交接.md`](项目交接/蕾米埃尔桌宠-统一项目解析与Agent交接.md)。它是本项目唯一的架构、Codex 接入、文件职责、验证和交接事实源。

- 结构查询先用 CodeGraph，再读它定位到的源码；不要从文件名猜架构。
- 默认不要扫描 `node_modules/`、`.codegraph/`、`dist/`、`动作视频/` 或二进制素材。
- 不要重新创建多份设计方案、实施计划、验收报告或临时交接书；发生有效变化时直接更新统一交接文件。
- 不得删除或重编码 `动作视频/` 原素材；发布前保留统一交接文件列出的当前发布物，新版完成构建、发布和回下载校验后才更新当前发布基线。
- “Hook 配置已写入”不等于“当前 Codex 桌面任务已加载”；真实桌面端复测前不得宣称正式接入完成。
- 修改代码后至少运行 `npm test`；只有发布变更才运行 `npm run build`。
- 每次有效项目更新通过测试后都必须按发布任务收尾：只关闭蕾米埃尔进程（不得关闭 Codex），递增版本并构建，安装或启动本地最新版，再提交并推送到 GitHub，创建对应版本标签并等待 GitHub Release 成功，最后回下载校验公开资产并把结果更新到统一交接文件。
